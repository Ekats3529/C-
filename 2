#include<iostream> 
#include<cmath> 
using namespace std; 
  
void f(int &x) { 
    x = (x % 100 == x % 1000 && x % 100 != x % 10) ? ((x % 10) * 10 + (x % 100 - x % 10) / 10) : x; 
} 

int main() 
{ 
    int a; 
    cin >> a; 
    f(a); 
    cout << a << endl; 
    return 0; 
} 
