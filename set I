#include <iostream>
#include <fstream>
#include <cmath>
#include <vector>
#include <list>
#include <set>
#include <string>
#include <algorithm>
using namespace std;


int main()
{
    set<int> a;
    int n, num;
    cin >> n;
    for (int t = 0; t < n; t++) {
        cin >> num;
        if (num < 10 || num > 999) { continue; }
        while (num > 9) {
            int odd = num % 10;
            a.insert(odd);
            num /= 10;
        }
        a.insert(num);
    }
    //cout << *a.end();
    set<int> ::iterator it = a.begin();
    for (int i = 0; i < 10; i++) {
        if (i == *a.rbegin()) { continue; }
        if ((*it) == i && *it != *a.rbegin()) { it++; }
        else { cout << i << " "; }
    }
    return 0;
}
