#include<iostream> 
#include<cmath> 
#include<iomanip> 
using namespace std;



int main()
{
    int m, n;
    cin >> n >> m;
    int **a = new int *[n];
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            cin >> a[i][j];
        }
    }
    int p = 1;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            p *= a[i][j];
        }
    }
    if (abs(p) > 99 && abs(p) < 1000) cout << "Yes";
    else cout << "No";
    return 0;
}

#include<iostream> 
#include<cmath> 
#include<iomanip> 
using namespace std;



int main()
{
    int m, n;
    cin >> n;
    int *a = new int [n];
    for (int i = 0; i < n; i++) {
            cin >> a[i];
    }
    int p = 1;
    for (int i = 0; i < n; i++) {
            p *= a[i];
    }
    if (abs(p) > 99 && abs(p) < 1000) cout << "Yes";
    else cout << "No";
    return 0;
}
